let socketio,http,path,fs;_cc4‍.w("socket.io",[["default",["socketio"],function(v){socketio=v}]]);_cc4‍.w("http",[["default",["http"],function(v){http=v}]]);_cc4‍.w("path",[["default",["path"],function(v){path=v}]]);_cc4‍.w("fs",[["default",["fs"],function(v){fs=v}]]);




const app = http.createServer(defaultHandler)
const io = socketio(app, {
  cors: {
    origin: '*',
    credentials: true
  }
})

const start = (port) => {
  console.log(`socket app is running on localhost:${port}`)
  app.listen(port)
}

const defaultHandler = (req, res) => {
  fs.readFile(path.join(__dirname, './index.html'), 'utf8',
    function (err, data) {
      if (err) {
        res.writeHead(500)
        return res.end('Error loading demo.html')
      }

      res.writeHead(200)
      res.end(data)
    })
}

start(9000)